apiVersion: v1
kind: Service
metadata:
  name: cap-availability
  labels:
    app.kubernetes.io/name: cap-availability

spec:
  type: LoadBalancer
  selector:
    app: cap-availability
  ports:
    - port: 80
      targetPort: 8080